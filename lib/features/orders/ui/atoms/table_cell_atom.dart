import 'package:flutter/material.dart';

class TableCellAtom extends StatelessWidget {
  final Widget child;

  const TableCellAtom(this.child);

  @override
  Widget build(BuildContext context) => child;
}